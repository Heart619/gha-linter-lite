import type { Report } from './types';
export declare function renderText(report: Report): string;
export declare function renderJson(report: Report): string;
export declare function renderMarkdown(report: Report): string;
