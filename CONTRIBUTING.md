# Contributing

## Local Setup

```sh
npm install
npm run check
```

## Pull Requests

- Keep each pull request focused on one rule or output behavior.
- Add tests for every new rule.
- Document user-visible rules and CLI options.
- Prefer warnings for advisory checks unless the workflow is structurally invalid.
